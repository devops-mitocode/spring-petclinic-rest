apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-rest-service
spec:
  selector:
    # type: NodePort
    app: spring-petclinic-rest
  ports:
    - protocol: "TCP"
      port: 9666
      targetPort: 9966
  type: NodePort